# Base URL for API calls. In dev, leave default and use Vite proxy (/api -> backend).
VITE_API_BASE=/api/v1

# Optional: where Vite proxies /api (vite.config.ts)
# VITE_API_PROXY_TARGET=http://localhost:4000
